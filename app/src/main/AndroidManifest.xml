<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taxi.taxitools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaxiTools">
        <activity android:name=".pantallas.ColaborativoActivity"></activity>
        <activity android:name=".pantallas.DomicilioActivity" />
        <activity android:name=".pantallas.SugerenciaRutas" />
        <activity android:name=".pantallas.Viajes" />
        <activity android:name=".pantallas.HomeActivity" />
        <activity android:name=".pantallas.MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>